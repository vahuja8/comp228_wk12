package com.va.week12.lab6;

public class AccountTest {
	
	/*
	 * Design a Main method. 
	 * 
	 * create an object for Account class 
	 * with specifying the intitial balance for the account. 
	 * 
	 *  Account account = new Account(1000);
	 *  
	 *  Create an arraylist object of RunnableType.. 
	 *  ArrayList<Runnable> transaction = new ArrayList<>();
	 *  
	 *  Scan input from the user.. using scanner object and parse
	 *  to the constructor 
	 *   transactions.add(new Transaction(ScannerObject1, Obj2, obj3));
	 *  ... do for 3-4 transaction.. 
	 *      transactions.add(new Transaction(account, "deposit", 200)); 
	 *      // above line is static.. -- change to dynamic. 
	 *  
	 *  
	 *  ExecutorService executorService = Executors.newFixedThreadPool(3);
	 *  
	 *  for (Runnable transaction : transactions) {	
            executorService.execute(transaction);
        }

        executorService.shutdown();
	 *  
	 */

	
	
}
